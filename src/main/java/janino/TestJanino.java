/*
package janino;

import java.io.StringReader;
import java.util.Scanner;

public class TestJanino {
    public static void main(String[] args) {  
        try {
            String code = "public int calculate(int r,int d){"
                    + "   return r + d "
                    + "      r[i]=d[0][i]+d[1][i]*2;" + "   }" + "}";  
 
            */
/*Calculater c = (Calculater) ExpressionEvaluator.createFastClassBodyEvaluator(new Scanner(null,
                            new StringReader(code)), // 要计算的表达式
                            Calculater.class, // 类型  
                            (ClassLoader) null// 当前线程的上下文类加载器  
                    );  *//*

 
            for (int i = 0; i < 5000; i++) {
                d0[i] = Math.floor(Math.random() * 100);
                d1[i] = Math.floor(Math.random() * 100);
            }
 
            double[][] d = new double[][] { d0, d1 };  

            c.calculate(r, d);  

        } catch (Exception e) {  
            e.printStackTrace();  
 
        }  
 
    }  
}*/
